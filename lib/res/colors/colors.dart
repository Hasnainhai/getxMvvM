import 'package:flutter/material.dart';

class AppColor {
  static const blackcolor = Color.fromARGB(0, 0, 0, 0);
  static const whitecolor = Color(0xffffffff);
  static const redcolor = Colors.red;
}
